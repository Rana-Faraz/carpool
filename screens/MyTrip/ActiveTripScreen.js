import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ActiveTripScreen = () => {
  return (
    <View>
      <Text>ActiveTripScreen</Text>
    </View>
  );
};

export default ActiveTripScreen;

const styles = StyleSheet.create({});
